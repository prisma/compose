/** Public surface: the `auth` extension pack. Implementation lives in `../pack/index.ts`. */
export {
  AUTH_PACK_HEAD_HASH,
  AUTH_PACK_ID,
  AUTH_SCHEMA,
  authPack,
  default,
} from '../pack/index.ts';

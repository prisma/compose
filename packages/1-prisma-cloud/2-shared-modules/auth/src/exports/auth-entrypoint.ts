/**
 * Public surface: the reusable auth boot program the service's build points
 * `entry` at. It exports nothing — importing it runs it. Implementation
 * lives in `../execution/auth-entrypoint.ts`.
 */
import '../execution/auth-entrypoint.ts';

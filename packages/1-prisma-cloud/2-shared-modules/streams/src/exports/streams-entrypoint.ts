/**
 * Public surface: the reusable streams boot program the service's build points
 * `entry` at. It exports nothing — importing it runs it. Implementation lives
 * in `../execution/streams-entrypoint.ts`.
 */
import '../execution/streams-entrypoint.ts';

{
  "name": "@makerkit/assemble",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Deploy-only: routes each service to its build adapter's /assemble entry and applies the wrapper-inlining policy. Consumed by @makerkit/cli; the future programmatic deploy API is its second consumer.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@makerkit/core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^24.10.13",
    "typescript": "^6.0.3"
  }
}
